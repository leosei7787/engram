# engram — core dependencies
# Install with: pip install -r requirements.txt
# Or for development: pip install -e ".[dev]"

pyyaml>=6.0
anthropic>=0.40.0
networkx>=3.0
python-louvain>=0.16
flask>=3.0
watchdog>=4.0          # folder watching (engram/ingest/watcher.py)

# Export skills (DOCX / PPTX / PDF generation)
python-docx>=1.1       # Word documents  (engram/export/converters.py)
python-pptx>=0.6.21    # PowerPoint      (engram/export/converters.py)
# weasyprint>=60       # optional: PDF via HTML (pip install weasyprint)
#                        if not installed, PDF exports fall back to styled HTML
